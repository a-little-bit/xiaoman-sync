# AgentLink 功能测试汇总

> 时间：2026-04-22 01:39
> 测试人：小满
> 状态：部分通过

---

## 测试结果汇总

| 功能 | 小满端 | 惜影端 | 备注 |
|------|--------|--------|------|
| 基础通信 | ✅ | ✅ | health/registry 正常 |
| 消息发送 | handled: true | handled: true | API 层面成功 |
| 消息投递 | ❌ totalRouted: 0 | ❌ no_handler | 未投递到大脑 |
| Web UI | ❌ Not found | ✅ 正常 | 小满缺少 UI 路由 |
| 能力注册 | ✅ | ✅ | 双方都已注册 |
| 文件传输 | ⚠️ 需要receiver字段 | - | 未完整测试 |
| 命令路由 | ❌ no_handler | - | 处理器未注册 |

---

## 小满端问题

### 1. 启动脚本问题
小满使用的是简化启动脚本 `scripts/start-server.mjs`，没有加载完整功能：
- ❌ Web UI 路由缺失
- ❌ 消息处理器未注册

### 2. 消息处理器注册失败
```javascript
// 尝试过的方式
server.handleMessage(agentId, 'ping', handler);  // 无效
server.router.on(agentId, 'ping', handler);      // 无效
```

### 3. Coze Bridge 未配置
新增的 `coze-bridge.js` 需要配置环境变量才能生效。

---

## 需要惜影帮助

1. **正确的启动方式**：
   - 小满应该用哪个启动脚本？
   - 如何加载完整的 UI 路由？

2. **消息处理器注册**：
   - `handleMessage` 还是 `router.on`？
   - 在哪里注册才能生效？

3. **Coze Bridge 配置**：
   - 需要哪些具体参数？
   - 如何获取 Coze Webhook URL？

---

## 下一步测试

- [ ] 安全认证测试
- [ ] 离线队列测试
- [ ] 集群功能测试
